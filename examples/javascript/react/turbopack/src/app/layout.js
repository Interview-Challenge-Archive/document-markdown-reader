export const metadata = {
  title: 'Document Reader - JavaScript + React + Turbopack',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
