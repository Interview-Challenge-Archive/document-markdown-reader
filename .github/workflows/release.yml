name: Release and Publish

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release-and-publish:
    name: Release and Publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0  # Fetch full history for PR detection
          fetch-tags: true

      - name: Get package info
        id: package_info
        run: |
          VERSION=$(node -p "require('./package.json').version")
          NODE_VERSION=$(node -p "require('./package.json').engines?.node || '20'")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "node_version=$NODE_VERSION" >> $GITHUB_OUTPUT
          echo "tag=v$VERSION" >> $GITHUB_OUTPUT

      - name: Check if tag exists
        id: check_tag
        run: |
          if git rev-parse "refs/tags/${{ steps.package_info.outputs.tag }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup Node.js
        if: steps.check_tag.outputs.exists == 'false'
        uses: actions/setup-node@v6.3.0
        with:
          node-version: ${{ steps.package_info.outputs.node_version }}
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        if: steps.check_tag.outputs.exists == 'false'
        run: npm install

      - name: Get latest release tag
        if: steps.check_tag.outputs.exists == 'false'
        id: last_tag
        run: |
          LAST_TAG=$(git tag --sort=-version:refname --list 'v[0-9]*.[0-9]*.[0-9]*' | head -1)
          echo "last_tag=$LAST_TAG" >> $GITHUB_OUTPUT

      - name: Generate Release Notes
        if: steps.check_tag.outputs.exists == 'false' && steps.last_tag.outputs.last_tag != ''
        id: release_notes
        continue-on-error: true
        uses: mikepenz/release-changelog-builder-action@v6.1.1
        with:
          configuration: ".github/release-notes-config.json"
          token: ${{ secrets.GITHUB_TOKEN }}
          fromTag: ${{ steps.last_tag.outputs.last_tag }}

      - name: Build release body
        if: steps.check_tag.outputs.exists == 'false'
        id: release_body
        env:
          CHANGELOG: ${{ steps.release_notes.outputs.changelog }}
          LAST_TAG: ${{ steps.last_tag.outputs.last_tag }}
          TAG: ${{ steps.package_info.outputs.tag }}
        run: |
          if [ -n "$CHANGELOG" ]; then
            echo "body<<EOF" >> $GITHUB_OUTPUT
            echo "$CHANGELOG" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          elif [ -n "$LAST_TAG" ]; then
            echo "body=No pull request changes detected since $LAST_TAG." >> $GITHUB_OUTPUT
          else
            echo "body=Initial release $TAG." >> $GITHUB_OUTPUT
          fi

      - name: Create GitHub Release
        if: steps.check_tag.outputs.exists == 'false'
        uses: ncipollo/release-action@v1.21.0
        with:
          tag: ${{ steps.package_info.outputs.tag }}
          name: Release ${{ steps.package_info.outputs.tag }}
          body: ${{ steps.release_body.outputs.body }}
          draft: false
          prerelease: false

      - name: Publish to npm
        if: steps.check_tag.outputs.exists == 'false'
        run: npm publish --provenance
