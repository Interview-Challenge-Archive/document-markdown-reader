name: Test

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  setup-matrix:
    name: Setup matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.setup-matrix.outputs.matrix }}


    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Setup Node.js matrix
        id: setup-matrix
        run: |
          # Get minimum supported major version from package.json.
          # Odd-numbered Node.js releases are not supported by the current test toolchain.
          MIN_VERSION=$(node -p "const match = require('./package.json').engines.node.match(/\\d+/); match ? match[0] : '20'")

          # Get latest LTS version from Node.js API
          LATEST_VERSION=$(curl -s https://nodejs.org/dist/index.json | jq -r '.[] | select(.lts) | .version' | head -n1 | cut -d'v' -f2)

          # Extract major version from latest LTS
          LATEST_MAJOR_VERSION=$(echo "$LATEST_VERSION" | cut -d'.' -f1)

          # Generate array of supported even major versions from MIN_VERSION to LATEST_MAJOR_VERSION
          if (( MIN_VERSION % 2 != 0 )); then
            MIN_VERSION=$((MIN_VERSION + 1))
          fi
          VERSIONS=""
          for ((i=MIN_VERSION; i<=LATEST_MAJOR_VERSION; i+=2)); do
            VERSIONS="$VERSIONS\"$i\","
          done
          # Remove trailing comma and wrap in brackets
          MATRIX="[${VERSIONS%,}]"

          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT

  test:
    needs: setup-matrix
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 2
      matrix:
        node-version: ${{ fromJson(needs.setup-matrix.outputs.matrix) }}

    name: Test with Node.js ${{ matrix.node-version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6.3.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test
